<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>RappyTV's Bot Stats</title>
        <meta property="theme-color" content="#5e17eb">
        <meta property="og:site_name" content="RappyTV's Bot Stats">
        <meta property="og:title" content="Main Page">
        <meta property="og:description" content="Here you can check out the guild counts of RappyTV's Bots">
        <meta property="og:image" content="/img/favicon.png">
        <meta property="og:url" content="<%= locals.base %>">
        <meta property="og:type" content="website">
        <link rel="stylesheet" href="/css/style.css">
        <link rel="icon" href="/img/favicon.png" type="image/png">
    </head>
    <body>
        <ul>
            <% locals.bots.forEach(bot => { %>
                <li>
                    <img src="<%= locals.icons.replace(`<id>`, bot.id) %>" alt="<%= bot.name %>">
                    <h1><%= bot.name %></h1>
                    <p><%= bot.guilds %></p>
                </li>
            <% }) %>
        </ul>
    </body>
</html>